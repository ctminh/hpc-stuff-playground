#ifndef __ALPHA_COMMON_H
#define __ALPHA_COMMON_H

#define ALPHA_SUCCESS  0
#define ALPHA_FAILURE -1

/**
 * Alpha common header.
 *
 * First let’s start with a header that will be common
 * to the server and the client. This header will simply
 * define the ALPHA_SUCCESS and ALPHA_FAILURE return codes.
 *
 */

#endif